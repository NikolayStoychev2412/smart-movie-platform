# Caddy automatically obtains and renews a Let's Encrypt TLS certificate.

moviemaze-api.duckdns.org {
    reverse_proxy api:8000 {
        header_up X-Forwarded-Proto {scheme}
        header_up X-Forwarded-Host {host}
    }
}
